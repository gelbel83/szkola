1. SELECT MIN(notatki.priorytet) FROM notatki WHERE Osoby_id = 3;
2. SELECT nazwa, priorytet FROM notatki WHERE nazwa LIKE "%na%";
3. SELECT osoby.imie, notatki.nazwa FROM notatki JOIN osoby ON notatki.Osoby_id = osoby.id WHERE notatki.priorytet = 5;
4. SELECT imie, COUNT(*) FROM osoby JOIN notatki on osoby.id = notatki.Osoby_id GROUP BY imie;
